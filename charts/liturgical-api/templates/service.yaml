apiVersion: v1
kind: Service
metadata:
  name: {{ include "liturgical-api.fullname" . }}
  labels:
    {{- include "liturgical-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liturgical-api.selectorLabels" . | nindent 4 }}
