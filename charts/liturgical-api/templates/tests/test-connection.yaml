apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liturgical-api.fullname" . }}-test-connection"
  labels:
    {{- include "liturgical-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liturgical-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
