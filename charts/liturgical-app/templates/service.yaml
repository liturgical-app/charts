apiVersion: v1
kind: Service
metadata:
  name: {{ include "liturgical-app.fullname" . }}
  labels:
    {{- include "liturgical-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liturgical-app.selectorLabels" . | nindent 4 }}
